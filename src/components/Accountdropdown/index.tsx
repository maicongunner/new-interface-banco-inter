import React from 'react';

import { Container } from './styles';

const Accountdropdown: React.FC = ({ children }) => {
  return (
    <Container>{children}</Container>
  )
}

export default Accountdropdown;
